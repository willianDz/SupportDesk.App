<?xml version="1.0" encoding="utf-8" ?>
<VerticalStackLayout
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="SupportDesk.App.Controls.Common.ErrorIndicator"
    IsVisible="False"
    Padding="48,12"
    Spacing="15">

    <Image
        x:Name="imgError"
        Source="{Binding ErrorImage, Mode=OneWay}"
        HorizontalOptions="Center"
        WidthRequest="64"
        HeightRequest="64"
        Aspect="AspectFit"/>

    <Label
        Text="Algo salió mal!"
        Style="{StaticResource MediumLightText18}"
        HorizontalOptions="Center"
        HorizontalTextAlignment="Center" />

    <Label
        x:Name="lblErrorText"
        Style="{StaticResource RegularLightText14}"
        LineBreakMode="WordWrap"
        HorizontalOptions="Center"
        HorizontalTextAlignment="Center" />

    <Button
        Text="Presionar para volver a intentarlo"
        Clicked="RetryIntialize_Clicked"
        FontSize="Micro"
        HorizontalOptions="Center" />

</VerticalStackLayout>
